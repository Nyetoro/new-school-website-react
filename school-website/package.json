{
  "name": "school-website",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack school website — React frontend + Express/SQLite backend",
  "scripts": {
    "setup": "npm run install:all && npm run seed",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && npm run seed",
    "start": "concurrently -n API,WEB -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "serve": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
